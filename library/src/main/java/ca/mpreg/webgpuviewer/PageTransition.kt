package ca.mpreg.webgpuviewer

import androidx.webgpu.GPUCommandEncoder
import androidx.webgpu.GPUTexture

class PageTransition {
    companion object {
        fun prepare(image: Image, dst: GPUTexture, x: Float, y: Float, scale: Float) {
        }

        fun basic(encoder: GPUCommandEncoder, dst: GPUTexture, x: Float, y: Float, scale: Float) {


        }
    }
}